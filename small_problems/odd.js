function isOdd(integer) {
	if (integer % 2 !== 0){
		return true;
	} else {
		return false
	}
}

/*
 **My omissions**
I didn't convert or make room for working with negative numbers.
When working with % (modulo/remainder operator) we should avoid working with negative 
numbers and only work with absolute numbers.

The Math.abs() static method returns the absolute value of a number.
*/

